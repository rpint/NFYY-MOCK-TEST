[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "my-mock-mcp"
version = "0.1.0"
description = "Mock Data MCP Server for Nexent"
dependencies = [
    "mcp>=1.0.0"
]

[project.scripts]
my-mock-mcp = "server:main"
